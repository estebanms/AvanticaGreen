<%= form_for(@infraction_type) do |f| %>
  <% if @infraction_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@infraction_type.errors.count, "error") %> prohibited this infraction_type from being saved:</h2>

      <ul>
      <% @infraction_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <% if display_form_field?(:name) %>
    <div class="field">
      <%= f.label :name %><br />
      <%= f.text_field :name %>
    </div>
  <% end %>
  <% if display_form_field?(:active) %>
    <div class="field">
      <%= f.check_box :active %> <%= f.label :active %>
    </div>
  <% end %>
  <% if display_form_field?(:points) %>
    <div class="field">
      <%= f.label :points %><br />
      <%= f.text_field :points %>
    </div>
  <% end %>
  <% if display_form_field?(:description) %>
    <div class="field">
      <%= f.label :description %><br />
      <%= f.text_area :description %>
    </div>
  <% end %>
  <% if display_form_field?(:hidden) %>
    <div class="field">
      <%= f.check_box :hidden %> <%= f.label :hidden %>
    </div>
  <% end %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
